name: Update GitHub Actions Dependencies

on:
  schedule:
    - cron: "0 0 * * 1"  # Runs every Monday
  workflow_dispatch:      # Manual trigger option

jobs:
  update-actions:
    runs-on: ubuntu-latest
    permissions:
      contents: write         # Required to modify repository contents
      pull-requests: write    # Required to create PRs
      actions: read           # Required to read workflow files

    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8  # v5.0.0
      - name: Update GitHub Actions
        uses: ThreatFlux/githubWorkFlowChecker@cbc8ccf2d1d480d438cfc71e1be68dd8a43b96f5  # v1.20250831.1
        with:
          owner: ${{ github.repository_owner }}
          repo-name: ${{ github.event.repository.name }}
          labels: "dependencies,security"
          token: ${{ secrets.GIT_TOKEN }}
